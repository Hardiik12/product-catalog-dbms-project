{
  "buildCommand": "if [ -d \"frontend-react/frontend-react\" ]; then cd frontend-react/frontend-react && npm install && npm run build; else npm run build; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
