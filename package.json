{
  "name": "product-catalog-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend-react/frontend-react && npm run dev",
    "build": "cd frontend-react/frontend-react && npm install && npm run build"
  }
}
