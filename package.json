{
  "name": "product-catalog-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend-react/frontend-react install && npm --prefix frontend-react/frontend-react run build"
  }
}
