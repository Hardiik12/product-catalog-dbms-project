{
  "name": "product-catalog-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "if [ -d \"frontend-react/frontend-react\" ]; then cd frontend-react/frontend-react && npm install && npm run build; else vite build; fi"
  }
}
